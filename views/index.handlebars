{{> navbar}}
<div class="content-section body">

    <!-- page header -->
    <header id="home" class="header body">
        <!-- Video background -->
        <canvas id="backgroundCanvas"
            style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; z-index: -1;"></canvas>
        <div class="overlay"></div>
        <div class="header-content container">
            <h1 class="header-title">
                <span class="up">Now it's time to start being professional!</span>
                <span class="down">Become global with us</span>
            </h1>
            <p class="header-subtitle"></p>
            <p class="header-subtitle"></p>

            <a href="Preview-form" class="btn btn-primary">Exclusive preview</a>
        </div>

    </header><!-- end of page header -->
    <div class="scroll-reveal body ">
        <canvas id="backgroundCanvas"
            style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; z-index: -1;"></canvas>

        {{> __about}}
    </div>

    <div class="scroll-reveal">
        {{> __service}}
    </div>

    <div class="scroll-reveal">
        {{> __darkCard}}
    </div>

    <div class="scroll-reveal">
        {{>container}}
    </div>

    <div class="scroll-reveal">
        {{> testimonial}}
    </div>


    <div class="scroll-reveal">
        {{> __pricing}}
    </div>


    <div class="scroll-reveal">
        {{> __Blogs}}
    </div>

    <div class="scroll-reveal">
        {{> FAQ}}
    </div>

    <div class="scroll-reveal">
        {{> contactus}}
    </div>

    {{> __footer}}



    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const revealSections = document.querySelectorAll('.scroll-reveal');

            function handleScroll() {
                revealSections.forEach((section) => {
                    const sectionTop = section.getBoundingClientRect().top;
                    const sectionBottom = section.getBoundingClientRect().bottom;
                    const triggerPoint = window.innerHeight * 0.8; // Adjust trigger point

                    // Show section when scrolled down
                    if (sectionTop < triggerPoint && sectionBottom > 0) {
                        section.classList.add('visible');
                    } else {
                        // Hide section when scrolled back up or out of view
                        section.classList.remove('visible');
                    }
                });
            }

            window.addEventListener('scroll', handleScroll);
            handleScroll(); // Run on page load to check the initial visibility
        });

    </script>


</div>